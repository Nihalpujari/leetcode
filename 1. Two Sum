class Solution(object):
    def twoSum(self, nums, target):
        x=0
        y=1
        my_list=[]
        while x<len(nums): 
            if nums[x] + nums[y] == target:
                return [x,y]
                break
            y+=1
            if y==len(nums):
                x+=1
                y=x+1

